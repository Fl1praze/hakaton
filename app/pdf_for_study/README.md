# 📁 Папка для обучающих PDF

## 📄 Назначение

Эта папка предназначена для хранения PDF-файлов счетов и чеков, которые будут использоваться для обучения модели извлечения данных.

---

## 🚀 Как использовать

### 1. Добавьте PDF файлы в эту папку

Просто скопируйте или перетащите ваши PDF чеки сюда:

```
app/pdf_for_study/
├── check_magnit_2025.pdf
├── invoice_yandex.pdf
├── receipt_sberbank.pdf
└── ...
```

### 2. Запустите извлечение данных

```bash
cd app
python auto_extract_training_data.py
```

Скрипт автоматически:
- Найдет все PDF в этой папке
- Извлечет текст из каждого файла
- Применит regex для поиска ИНН, названия, даты, суммы
- Сохранит результаты в `training_data.json`

### 3. Обучите модель

```bash
python train_and_save_model.py
```

---

## 📋 Какие PDF подходят

**Подходят:**
- ✅ Платежные счета
- ✅ Кассовые чеки
- ✅ Электронные чеки из email
- ✅ Скриншоты чеков (если сохранены как PDF)

**Требования:**
- Формат: PDF
- Язык: Русский
- Качество: Читаемый текст (или хорошее качество для OCR)

---

## 🔍 Что извлекается

Из каждого PDF извлекаются следующие поля:

| Поле | Описание | Пример |
|------|----------|--------|
| `inn` | ИНН организации (10-12 цифр) | `7728867666` |
| `vendor` | Название компании | `АО ТАНДЕР` |
| `date` | Дата чека | `27.09.2025` |
| `total` | Итоговая сумма | `692.88` |

---

## 📊 Рекомендации

**Для лучшего качества обучения:**

1. **Разнообразие форматов**
   - Добавляйте чеки из разных магазинов/сервисов
   - Разные форматы дат, сумм, названий

2. **Количество**
   - Минимум: 20-30 примеров
   - Оптимально: 50-100 примеров
   - Больше = лучше точность модели

3. **Качество**
   - Читаемый текст
   - Не обрезанные документы
   - Корректная информация

---

## ⚠️ Важно

**Файлы в этой папке НЕ попадают в Git:**
- PDF файлы игнорируются (см. `.gitignore`)
- Это сделано для безопасности (в чеках могут быть личные данные)
- В Git сохраняется только структура папки (этот README)

---

## 🆘 Проблемы?

**Q: Скрипт не находит PDF файлы**
- Убедитесь что файлы имеют расширение `.pdf` (не `.PDF`)
- Проверьте что файлы находятся в `app/pdf_for_study/`

**Q: Низкая точность извлечения**
- Добавьте больше примеров (50-100)
- Увеличьте количество эпох обучения (50-100)
- Проверьте качество PDF (возможно нужен лучший OCR)

**Q: Ошибка "tesseract not found"**
- Установите Tesseract OCR (см. главный README)
- Или работайте только с текстовыми PDF (не отсканированными)

---

## 📝 Пример workflow

```bash
# 1. Добавьте PDF в эту папку (вручную)

# 2. Перейдите в app
cd app

# 3. Извлеките данные
python auto_extract_training_data.py
# Результат: training_data.json с 27 примерами

# 4. Обучите модель
python train_and_save_model.py
# Результат: models/invoice_extractor.pkl (92% точность)

# 5. Запустите API
cd ..
uvicorn app.main:app --reload
# API готов к работе с обученной моделью!
```

---

✅ **Готово! Добавляйте PDF и начинайте обучение!**

